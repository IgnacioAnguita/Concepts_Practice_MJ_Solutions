#ifndef RANDOM_H    // To make sure you don't declare the function more than once by including the header multiple times.
#define RANDOM_H

double GetOneGaussianBySummation();
double GetOneGaussianByBoxMuller();

#endif
